import { expect } from 'chai';
import sinon from 'sinon';
import { describe, it } from 'mocha';
import { dbClient } from '../../utils/db';

describe('dbClient test must rewrite for clarity', () => {
  it('test not available', () => {
    expect(true).to.be(true);
  });
});
